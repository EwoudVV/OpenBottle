# OpenBottle 0.1.0 alpha 4

this build fixes the save-vault path check exposed by the first real Screw
Drivers Play click.

the snapshot had copied and verified correctly, but Foundation represented its
parent URL differently from the already-stored vault URL. comparing the URL
objects rejected the valid snapshot before Steam started. the final boundary
check now compares normalized filesystem paths, matching the vault's existing
descendant check.

OpenBottle 0.1.0 build 4 keeps the bounded-memory importer and hand-drawn icon.
it remains ad-hoc signed and not notarized.
